org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
# Kotlin sources in the libraries' test fixtures (src/testFixtures, #199). AGP
# still gates Kotlin there behind this flag; a host that copies the libraries
# sets it too (docs/library-consumption.md).
android.experimental.enableTestFixturesKotlinSupport=true
