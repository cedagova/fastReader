# Plans (historical record)

Implementation plans as they were approved. Each is a record of its time: its
status line is the status at approval, and the code it names may have moved
since. The first one, `cedagova-fastReader-1/`, was delivered (issue #1 is
closed). Later plans live on their own planning branches and PRs.

The current map of the code is [docs/architecture.md](../architecture.md).
